import React from "react";

const Form = ()=>{
    return(
        <form >
            <input type={"text"} placeholder={"Wpisz miasto"}/>
            <button>Wyszukaj miasto</button>
        </form>
    )
};

export default Form
